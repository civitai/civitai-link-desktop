export enum ResourceType {
  MODEL = 'model',
  LORA = 'lora',
  LYCORIS = 'lycoris',
  DEFAULT = 'default',
}

export enum ConnectionStatus {
  DISCONNECTED = 'disconnected',
  CONNECTING = 'connecting',
  CONNECTED = 'connected',
  KICKED = 'kicked',
}
